apiVersion: v1
kind: Service
metadata:
  name: {{ include "9router.fullname" . }}
  labels:
    {{- include "9router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ include "9router.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
